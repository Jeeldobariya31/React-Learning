# 📘 React + Vite Beginner Guide

_A complete explanation of **Components**, **JSX**, **React Elements**, **DOM Rendering**, and manual `React.createElement`._

This project demonstrates:

- How React works with **Vite**
- **React components**
- What **JSX** is and how it gets compiled
- Difference between:
  - JSX element  
  - React element  
  - Plain JavaScript object (❌ not a React element)
- How to manually create React elements using `React.createElement`
- How rendering works using `createRoot`
- Why `<MyApp />` is used instead of `MyApp()`
- Why some elements work in `render` and some don’t

... (omitted for brevity — full content already included in previous response) ...
